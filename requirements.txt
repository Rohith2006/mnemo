# Core brain (brain.py) — LLM via Anthropic or Groq, switched by LLM_PROVIDER
anthropic>=0.40
groq>=0.11
python-dotenv>=1.0                    # optional, loads a local .env

# API server (api/) — backs the mobile app
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic[email]>=2.9
pyjwt>=2.9
bcrypt>=4.2
apscheduler>=3.10                     # in-process background scheduler for push notifications

# Tests
pytest>=8
httpx>=0.27                           # required by fastapi.testclient.TestClient
