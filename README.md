# mnemo

A hyper-personalized AI assistant with persistent long-term memory. Full
architecture and setup docs land in a later stage of the rebuild — for now:

```bash
python -m venv venv
venv\Scripts\activate        # Windows
pip install -r requirements.txt
cp .env.example .env         # then fill in your keys
```
